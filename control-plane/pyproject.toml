# SPDX-License-Identifier: Apache-2.0

[project]
name = "teremoq-control-plane"
version = "0.1.0"
description = "Deterministic, provider-neutral Teremoq control-plane simulator"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
teremoq-control-plane = "teremoq_control.cli:main"
